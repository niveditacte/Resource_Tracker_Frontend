{
  "buildCommand": "ng build Resource_Tracker --configuration production",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "dist/resource-tracker",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
